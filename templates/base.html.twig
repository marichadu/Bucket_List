<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css">
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
        <style>
            /* Body Background */
            body {
                background: url('/12.png') no-repeat center center fixed; /* Static background image */
                background-size: cover; /* Ensure the image covers the entire page */
                color: #fff; /* Default text color for readability */
                font-family: 'Montserrat', sans-serif;
            }

            /* Navbar */
            .navbar {
                background-color: #ffffff;
                padding: 20px 20px;
                position: sticky;
                top: 0;
                z-index: 1000;
                border: none;
                box-shadow: none;
            }
            .navbar-brand {
                font-family: 'Playfair Display', serif;
                font-weight: 700;
                font-size: 1.8rem;
                color: #333 !important;
            }
            .navbar-nav .nav-link {
                font-family: 'Playfair Display', serif;
                font-weight: 600;
                font-size: 1rem;
                text-align: center;
                margin-left: 20px; /* Add spacing between the brand and the links */
            }
            .navbar-nav .nav-link:first-child {
                color: #000 !important; /* Black for the first menu item */
            }
            .navbar-nav .nav-link:not(:first-child) {
                color: #f8d210 !important; /* Yellow for other menu items */
            }
            .navbar-nav .nav-link:hover {
                color: #ffc107 !important; /* Highlight color on hover */
            }
            .navbar-toggler img {
                width: 30px;
                height: 30px;
            }

            /* Hero Section */
            .hero {
                background: url('/12.png') no-repeat center center/cover;
                background-attachment: fixed;
                color: white;
                text-align: center;
                height: calc(100vh - 70px - 40px);
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                padding: 0 20px;
                position: relative;
            }
            .hero h1 {
                font-size: 4rem;
                font-weight: 700;
                margin-bottom: 20px;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
                font-family: 'Playfair Display', serif;
            }
            .hero p {
                font-size: 1.5rem;
                margin-bottom: 30px;
                text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.7);
                font-family: 'Playfair Display', sans-serif;
            }
            .hero .btn-primary {
                background-color: #f8d210;
                border-color: #f8d210;
                color: #000;
                font-weight: 600;
                padding: 10px 30px;
                border-radius: 50px;
                transition: all 0.3s ease;
            }
            .hero .btn-primary:hover {
                background-color: #ffc107;
                border-color: #ffc107;
                transform: scale(1.05);
            }



            .card {
                background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background */
                border: none; /* Remove border for a cleaner look */
                border-radius: 10px; /* Add rounded corners */
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
            }
            .card-title {
                font-family: 'Playfair Display', serif;
                font-weight: 700;
                font-size: 1.5rem;
            }
            .card-text {
                font-family: 'Montserrat', sans-serif;
                font-size: 1rem;
                color: #555;
            }


            .btn-primary {
                background-color: #ffc107 !important; /* Bootstrap primary color */
                border-color: #ffc107 !important; /* Match the border color */
                color: #fff !important; /* Ensure the text color is white */
            }
            
            .btn-primary:hover {
                background-color: #ffc107 !important; /* Bootstrap hover color */
                border-color: #ffc107 !important; /* Match the hover border color */
            }



            /* Registration Form Styling */
            .registration-form .form-label {
                font-weight: 600;
                color: #333;
            }
            
            .registration-form .form-control {
                border-radius: 8px;
                border: 1px solid #ccc;
                padding: 12px;
                font-size: 1rem;
                margin-bottom: 20px; /* Add spacing between fields */
            }
            
            .registration-form .form-control:focus {
                border-color: #ffc107;
                box-shadow: 0 0 5px rgba(255, 193, 7, 0.5);
            }
            
            .registration-form .btn-primary {
                background-color: #f8d210;
                border-color: #f8d210;
                color: #000;
                font-weight: 600;
                padding: 10px 30px;
                border-radius: 50px;
                transition: all 0.3s ease;
            }
            
            .registration-form .btn-primary:hover {
                background-color: #ffc107;
                border-color: #ffc107;
                transform: scale(1.05);
            }

            /* Footer */
            footer {
                background-color: #ffffff;
                color: #333;
                text-align: center;
                padding: 20px 0;
                margin-top: 40px;
            }
            footer a {
                color: #f8d210;
                text-decoration: none;
                font-weight: 500;
            }
            footer a:hover {
                text-decoration: underline;
            }
            </style>
    </head>
    <body>
        <nav class="navbar navbar-expand-md">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_bucket_list') }}">Bucket List</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <img src="/55.jpg" alt="Menu">
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_bucket_list_new') }}">Add New Item</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_bucket_list_suggestions') }}">Suggestions</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_register') }}">Sign Up</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <!-- Flash Messages -->
        <div class="container mt-4">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">{{ message }}</div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger">{{ message }}</div>
            {% endfor %}
        </div>

        <!-- Main Content -->
        {% block body %}{% endblock %}

        <!-- Footer -->
        <footer>
            <p>&copy; 2025 Bucket List. Built with ❤️ by <a href="#">Mariami</a>.</p>
        </footer>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
        <script>
            AOS.init({
                duration: 1000,
                once: true,
            });
        </script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
